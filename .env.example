# Optional: when set, ads are generated by OpenAI.
# When unset, the app falls back to a built-in deterministic generator
# so the demo works out of the box.
OPENAI_API_KEY=

# Optional: model to use. Defaults to gpt-4o-mini.
OPENAI_MODEL=gpt-4o-mini
